import { colorSchemeLightNeutral, corePart, iconSetQuartz } from '..';
import { combineParts } from '../theme-utils';

export const quartzTheme = combineParts([corePart, colorSchemeLightNeutral, iconSetQuartz]);
